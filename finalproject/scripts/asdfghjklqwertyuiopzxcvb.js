try {let cYear = document.getElementById("currentyear");let date = new Date();cYear.textContent = date.getFullYear();let lModified = document.querySelector("#lastModified");let lDate = new Date(2025,9,13);lModified.innerText = lDate;let toggle = document.querySelector(".toggle");let show = document.querySelector(".hide");toggle.addEventListener('click', () => {show.classList.toggle("hide");toggle.classList.toggle("show");});}catch (error) {console.log(error);}let sect = document.querySelector(".sc");let vw = document.querySelector(".view");let fname = document.querySelector("#firstName");let lname = document.querySelector("#lastName");let email = document.querySelector("#email");let tel = document.querySelector("#tel");let desc = document.querySelector("#description");let sub = document.querySelector("#sub");let wp = document.querySelector(".wp");let btnvw = document.querySelector("#btnvw");try {sub.addEventListener("click", () => {localStorage.setItem('tp-data', JSON.stringify([fname.value, lname.value, email.value, tel.value, desc.value]));});}catch (error) {console.log(error);}try{let tp = JSON.parse(localStorage.getItem('tp-data'));wp.textContent = `Welcome ${tp[0].toUpperCase()} ${tp[1].toUpperCase()}`;}catch (error) {console.log(error);}function view() {vw.classList.add("--");vw.classList.remove("view");}try{btnvw.addEventListener("click", () => {view();});}catch (error) {console.log(error);}let createCard = (arr) => {arr.forEach(i => {let aside = document.createElement("aside");let img = document.createElement("img");img.setAttribute("src", i.image);let btn = document.createElement("button");btn.setAttribute("id", "btn");btn.textContent = "Details";let diag = document.createElement("dialog");let h2 = document.createElement("h2");h2.textContent = "TECH POSIBILITIES";let btn1 = document.createElement("button");p = document.createElement("p");p.innerHTML = `<span>Name: </span> ${i.name} <br> <span>Price: </span> ${i.price}`;aside.appendChild(img);aside.appendChild(btn);aside.appendChild(diag);diag.appendChild(h2);diag.appendChild(btn1);diag.appendChild(p);sect.appendChild(aside);btn.addEventListener("click", () => {diag.showModal();});btn1.addEventListener("click", () => {diag.close();})})};async function getData() {try{const response = await fetch("https://ugwu-chukwuebuka-martins.github.io/wdd231/finalproject/data/data.json");const data = await response.json();createCard(data);}catch (error) {console.log(error);}}getData();